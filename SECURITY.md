# Security policy

Do not report suspected credential exposure, configuration overwrite, gateway authentication bypass, or local data disclosure in a public issue. Until a dedicated security contact is configured, open a private GitHub security advisory for this repository with reproduction steps that exclude real credentials.

We aim to acknowledge reports within seven days. Never include API keys, authorization headers, prompts, or user configuration contents in a report.
